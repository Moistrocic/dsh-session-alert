# 统一按 LF 存储；避免 core.autocrlf=true 带来的反复转换与警告。
* text=auto eol=lf

# Windows 上确实需要 CRLF 的文件（目前没有，留作约定）
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制，不做任何换行与编码处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.wav binary
*.zip binary
*.exe binary
*.dll binary
