# 统一按 LF 存储；避免 core.autocrlf=true 带来的反复转换与警告。
* text=auto eol=lf

# PowerShell 脚本必须用 CRLF：LF 换行会让 Windows PowerShell 5.1 的
# param(...) 块解析失败（报 "Unexpected token ')'"）。此处显式声明，
# 使脚本以 CRLF 检出，并在提交时归一化——否则每次改动都会整文件 diff。
*.ps1 text eol=crlf
*.psm1 text eol=crlf
*.psd1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制，不做任何换行与编码处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.wav binary
*.zip binary
*.exe binary
*.dll binary
